--find-links https://data.dgl.ai/wheels/repo.html
mcp
python-dotenv
pytest
pubchempy
requests
mp-api
pymatgen
ase
matminer
dscribe
numpy<2
torch==2.2.1
torchdata==0.7.1
dgl<2 # windows requires dgl<2, linux/mac can use latest versions
matgl
transformers<=4.49
accelerate
lxml  # optional: used for pretty-printing fixed URDF XML
trimesh  # lula tool: mesh loading and voxelisation for collision sphere generation
scipy    # lula tool: Euclidean distance transform for sphere packing
pymeshfix  # optional, lula tool: improves mesh repair quality (falls back to trimesh)
pyyaml     # lula tool: YAML output (also transitively required by pymatgen)





# enumlib (enum.x) is required for pymatgen_enumeration_generator but is NOT on PyPI.
# It is installed automatically by setup.sh (via conda) on Linux/macOS/WSL.
# Manual install: conda install -c conda-forge enumlib
# Windows without WSL: not supported — see README.md.

